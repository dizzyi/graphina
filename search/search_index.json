{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Graphina","text":"<p>Graphina is a high-performance graph data science library for Rust \ud83e\udd80. It provides a robust collection of graph algorithms, data structures, and analytics tools designed for efficiency and ease of use.</p>"},{"location":"#features","title":"Features","text":"<ul> <li>Flexible and efficient <code>Graph</code> (undirected) and <code>Digraph</code> (directed) implementations.</li> <li>Optimized for speed and memory usage, with parallel implementations for heavy workloads.</li> <li>Includes a comprehensive collection of graph algorithms including Centrality, Community Detection, Path Finding, and more.</li> <li>Has ready-to-use Python bindings to use Graphina from Python \ud83d\udc0d.</li> </ul>"},{"location":"#using-graphina-from-python","title":"Using Graphina from Python","text":"<p>Using Python? Check out PyGraphina documentation for more information.</p>"},{"location":"#quick-links","title":"Quick Links","text":"<ul> <li>Getting Started</li> <li>Rust API Docs (docs.rs)</li> <li>GitHub Repository</li> </ul>"},{"location":"examples/approximation/","title":"Approximation Algorithms Examples","text":"<p>Examples of using Graphina to solve NP-hard problems using approximation heuristics.</p>"},{"location":"examples/approximation/#solving-traveling-salesman-problem-tsp","title":"Solving Traveling Salesman Problem (TSP)","text":"<p>Find an approximate shortest path that visits every node exactly once.</p> <pre><code>use graphina::core::types::Graph;\nuse graphina::approximation::tsp::greedy_tsp;\nuse ordered_float::OrderedFloat;\n\nfn main() {\n    // Note: TSP generally requires a complete or dense graph with metric weights.\n    // Weights must be Ord, so we use OrderedFloat&lt;f64&gt;.\n    let mut graph = Graph::&lt;&amp;str, OrderedFloat&lt;f64&gt;&gt;::new();\n\n    let a = graph.add_node(\"A\");\n    let b = graph.add_node(\"B\");\n    let c = graph.add_node(\"C\");\n    let d = graph.add_node(\"D\");\n\n    // Add edges forming a cycle/tour\n    graph.add_edge(a, b, OrderedFloat(1.0));\n    graph.add_edge(b, c, OrderedFloat(1.0));\n    graph.add_edge(c, d, OrderedFloat(1.0));\n    graph.add_edge(d, a, OrderedFloat(1.0));\n    // Cross edges\n    graph.add_edge(a, c, OrderedFloat(1.5));\n    graph.add_edge(b, d, OrderedFloat(1.5));\n\n    let start_node = a;\n\n    if let Ok((tour, cost)) = greedy_tsp(&amp;graph, start_node) {\n        println!(\"Tour order: {:?}\", tour);\n        println!(\"Total cost: {}\", cost);\n    } else {\n        println!(\"No tour found (graph might not be connected)\");\n    }\n}\n</code></pre>"},{"location":"examples/approximation/#minimizing-vertex-cover","title":"Minimizing Vertex Cover","text":"<p>Find the smallest set of nodes that \"covers\" (touches) every edge in the graph.</p> <pre><code>use graphina::core::types::Graph;\nuse graphina::approximation::vertex_cover::min_vertex_cover;\n\nfn main() {\n    let mut graph = Graph::&lt;i32, f64&gt;::new();\n    let n1 = graph.add_node(1);\n    let n2 = graph.add_node(2);\n    graph.add_edge(n1, n2, 1.0);\n\n    let cover = min_vertex_cover(&amp;graph);\n    println!(\"Vertex cover size: {}\", cover.len());\n    println!(\"Nodes in cover: {:?}\", cover);\n}\n</code></pre>"},{"location":"examples/approximation/#finding-maximum-clique","title":"Finding Maximum Clique","text":"<p>Find a large clique (subset of fully connected nodes) in the graph.</p> <pre><code>use graphina::core::types::Graph;\nuse graphina::approximation::clique::max_clique;\n\nfn main() {\n    let mut graph = Graph::&lt;i32, f64&gt;::new();\n    let n1 = graph.add_node(1);\n    let n2 = graph.add_node(2);\n    let n3 = graph.add_node(3);\n\n    // Create a clique\n    graph.add_edge(n1, n2, 1.0);\n    graph.add_edge(n2, n3, 1.0);\n    graph.add_edge(n3, n1, 1.0);\n\n    let clique = max_clique(&amp;graph);\n    println!(\"Found clique of size: {}\", clique.len());\n}\n</code></pre>"},{"location":"examples/basic/","title":"Basic Examples","text":"<p>This example demonstrates how to build a social network analysis tool using Graphina. Use a custom struct for node data, a powerful feature of Graphina's generic system.</p>"},{"location":"examples/basic/#social-network-analysis","title":"Social Network Analysis","text":"<p>We'll model a network where:</p> <ul> <li>Nodes: Users with names and ages.</li> <li>Edges: Friendships with a \"strength\" score.</li> </ul> <pre><code>use graphina::core::types::Graph;\nuse graphina::centrality::pagerank;\nuse std::fmt;\n\n// Define your custom node data\n#[derive(Clone, Debug, PartialEq)]\nstruct User {\n    name: String,\n    age: u32,\n}\n\nimpl fmt::Display for User {\n    fn fmt(&amp;self, f: &amp;mut fmt::Formatter) -&gt; fmt::Result {\n        write!(f, \"{} ({})\", self.name, self.age)\n    }\n}\n\nfn main() {\n    // 1. Create the graph\n    let mut social_graph = Graph::&lt;User, f64&gt;::new();\n\n    // 2. Add users\n    let alice = social_graph.add_node(User { name: \"Alice\".to_string(), age: 30 });\n    let bob = social_graph.add_node(User { name: \"Bob\".to_string(), age: 25 });\n    let charlie = social_graph.add_node(User { name: \"Charlie\".to_string(), age: 35 });\n    let dave = social_graph.add_node(User { name: \"Dave\".to_string(), age: 28 });\n\n    // 3. Add friendships (weights represent closeness)\n    social_graph.add_edge(alice, bob, 0.9);\n    social_graph.add_edge(alice, charlie, 0.5);\n    social_graph.add_edge(bob, charlie, 0.8);\n    social_graph.add_edge(charlie, dave, 0.3);\n\n    // 4. Analyze: Find the \"Influencer\" of the group using PageRank\n    let ranks = pagerank(&amp;social_graph, 0.85, 100, 1e-6, None).unwrap();\n\n    println!(\"Social Influence Ranking:\");\n\n    // Sort results\n    let mut sorted_ranks: Vec&lt;_&gt; = ranks.iter().collect();\n    sorted_ranks.sort_by(|a, b| b.1.partial_cmp(a.1).unwrap());\n\n    for (node_id, score) in sorted_ranks {\n        let user = &amp;social_graph[*node_id];\n        println!(\"- {}: {:.4}\", user, score);\n    }\n\n    // 5. Explore: Who represents the 'center' of the network?\n    // (Using density as a simple metric of connectivity)\n    println!(\"\\nNetwork Density: {:.2}\", social_graph.density());\n}\n</code></pre>"},{"location":"examples/centrality/","title":"Centrality Examples","text":"<p>Use various centrality measures to identify important nodes in a graph.</p>"},{"location":"examples/centrality/#degree-centrality","title":"Degree Centrality","text":"<p>Computes the centrality of nodes based on the number of connections.</p> <pre><code>use graphina::centrality::degree::{degree_centrality, in_degree_centrality, out_degree_centrality};\nuse graphina::core::types::{Graph, Digraph};\n\nfn main() {\n    // 1. Undirected Degree\n    let mut g = Graph::new();\n    let nodes = [g.add_node(1), g.add_node(2), g.add_node(3)];\n    g.add_edge(nodes[0], nodes[1], ());\n    g.add_edge(nodes[0], nodes[2], ());\n\n    let cent = degree_centrality(&amp;g).unwrap();\n    println!(\"Node 1 degree: {}\", cent[&amp;nodes[0]]); // 2.0\n\n    // 2. Directed In/Out Degree\n    let mut dg = Digraph::new();\n    let dnodes = [dg.add_node(1), dg.add_node(2)];\n    dg.add_edge(dnodes[0], dnodes[1], ());\n\n    let in_cent = in_degree_centrality(&amp;dg).unwrap();\n    let out_cent = out_degree_centrality(&amp;dg).unwrap();\n\n    println!(\"Node 1 Out-Degree: {}\", out_cent[&amp;dnodes[0]]); // 1.0\n    println!(\"Node 2 In-Degree: {}\", in_cent[&amp;dnodes[1]]);   // 1.0\n}\n</code></pre>"},{"location":"examples/centrality/#pagerank","title":"PageRank","text":"<p>Detailed example of computing PageRank on a directed graph.</p> <pre><code>use graphina::centrality::pagerank::pagerank;\nuse graphina::core::types::Digraph;\n\nfn page_rank_example() {\n    let mut graph = Digraph::new();\n    let nodes = (0..5).map(|i| graph.add_node(i)).collect::&lt;Vec&lt;_&gt;&gt;();\n\n    // Create a citation-like structure\n    let edges = [(0, 1, 1.0), (0, 2, 1.0), (1, 3, 1.0)];\n    for (s, d, w) in edges {\n        graph.add_edge(nodes[s], nodes[d], w);\n    }\n\n    // Compute PageRank (damping: 0.85, iter: 1000, tol: 1e-6)\n    let scores = pagerank(&amp;graph, 0.85, 1000, 1e-6_f64, None).unwrap();\n\n    for n in nodes {\n        println!(\"Node {:?}: {:.4}\", n, scores[&amp;n]);\n    }\n}\n</code></pre>"},{"location":"examples/centrality/#eigenvector-centrality","title":"Eigenvector Centrality","text":"<p>Measures influence by looking at connections to other high-scoring nodes.</p> <pre><code>use graphina::centrality::eigenvector::eigenvector_centrality;\nuse graphina::core::types::Graph;\n\nfn main() {\n    let mut g = Graph::new();\n    let nodes = [g.add_node(1), g.add_node(2), g.add_node(3)];\n    g.add_edge(nodes[0], nodes[1], 1.0);\n    g.add_edge(nodes[0], nodes[2], 2.0);\n\n    let scores = eigenvector_centrality(&amp;g, 1000, 1e-6).expect(\"Failed to converge\");\n\n    // Node 0 is connected to both 1 and 2, so it should be most central\n    assert!(scores[&amp;nodes[0]] &gt; scores[&amp;nodes[1]]);\n}\n</code></pre>"},{"location":"examples/centrality/#katz-centrality","title":"Katz Centrality","text":"<p>A generalization of degree centrality that counts paths of all lengths, penalized by an attenuation factor \\(\\alpha\\).</p> <pre><code>use graphina::centrality::katz::katz_centrality;\nuse graphina::core::types::Graph;\n\nfn main() {\n    let mut g = Graph::new();\n    let nodes = [g.add_node(1), g.add_node(2), g.add_node(3)];\n    g.add_edge(nodes[0], nodes[1], 1.0);\n    g.add_edge(nodes[0], nodes[2], 2.0);\n\n    // alpha = 0.1, beta = 1.0\n    let scores = katz_centrality(&amp;g, 0.1, Some(&amp;|_| 1.0), 1000, 1e-6).unwrap();\n\n    println!(\"Node 1 Katz Score: {:.4}\", scores[&amp;nodes[0]]);\n}\n</code></pre>"},{"location":"examples/centrality/#closeness-centrality","title":"Closeness Centrality","text":"<p>Based on the average shortest path distance to all other nodes.</p> <pre><code>use graphina::centrality::closeness::closeness_centrality;\nuse graphina::core::types::Graph;\nuse ordered_float::OrderedFloat;\n\nfn main() {\n    let mut graph: Graph&lt;i32, OrderedFloat&lt;f64&gt;&gt; = Graph::new();\n    let nodes = (0..5).map(|i| graph.add_node(i)).collect::&lt;Vec&lt;_&gt;&gt;();\n    let edges = [\n        (0, 1, OrderedFloat(1.0)),\n        (0, 2, OrderedFloat(1.0)),\n        (1, 3, OrderedFloat(1.0))\n    ];\n\n    for (s, d, w) in edges {\n        graph.add_edge(nodes[s], nodes[d], w);\n    }\n\n    let scores = closeness_centrality(&amp;graph).unwrap();\n    println!(\"Node 0 Closeness: {:.4}\", scores[&amp;nodes[0]]);\n}\n</code></pre>"},{"location":"examples/community/","title":"Community Detection Examples","text":"<p>This guide demonstrates how to use Graphina's community detection algorithms to find clusters in your data.</p>"},{"location":"examples/community/#detecting-communities-with-louvain-method","title":"Detecting Communities with Louvain Method","text":"<p>The Louvain method is excellent for finding modular communities in large networks.</p> <pre><code>use graphina::core::types::Graph;\nuse graphina::community::louvain;\nuse std::collections::HashMap;\n\nfn main() {\n    // 1. Create a graph (e.g., a small social circle)\n    let mut graph = Graph::&lt;&amp;str, f64&gt;::new();\n    let n0 = graph.add_node(\"Alice\");\n    let n1 = graph.add_node(\"Bob\");\n    let n2 = graph.add_node(\"Charlie\"); // Cluster 1\n\n    let n3 = graph.add_node(\"Dave\");\n    let n4 = graph.add_node(\"Eve\");     // Cluster 2\n\n    // Connect Cluster 1\n    graph.add_edge(n0, n1, 1.0);\n    graph.add_edge(n1, n2, 1.0);\n    graph.add_edge(n0, n2, 1.0);\n\n    // Connect Cluster 2\n    graph.add_edge(n3, n4, 1.0);\n\n    // Weak connection between clusters\n    graph.add_edge(n2, n3, 0.2);\n\n    // 2. Run Louvain\n    let communities = louvain(&amp;graph);\n\n    // 3. Print results\n    // Organize by community ID\n    let mut groups: HashMap&lt;usize, Vec&lt;&amp;str&gt;&gt; = HashMap::new();\n    for (node_id, comm_id) in communities {\n        let label = graph.node_weight(node_id).unwrap();\n        groups.entry(comm_id).or_default().push(label);\n    }\n\n    for (id, members) in groups {\n        println!(\"Community {}: {:?}\", id, members);\n    }\n}\n</code></pre>"},{"location":"examples/community/#girvan-newman-for-hierarchical-clustering","title":"Girvan-Newman for Hierarchical Clustering","text":"<p>If you need to understand the hierarchy of splits (e.g., separating a graph into exactly 2 communities), Girvan-Newman is useful.</p> <pre><code>use graphina::core::types::Graph;\nuse graphina::community::girvan_newman::girvan_newman;\n\nfn main() {\n    let mut graph = Graph::&lt;&amp;str, f64&gt;::new();\n    let n1 = graph.add_node(\"A\");\n    let n2 = graph.add_node(\"B\");\n    let n3 = graph.add_node(\"C\");\n\n    // A triangle\n    graph.add_edge(n1, n2, 1.0);\n    graph.add_edge(n2, n3, 1.0);\n    graph.add_edge(n3, n1, 1.0);\n\n    // Split into exactly 2 communities (might fail on a strict triangle, so we add a disconnected node)\n    let n4 = graph.add_node(\"D\");\n\n    match girvan_newman(&amp;graph, 2) {\n        Ok(communities) =&gt; {\n            for (i, comm) in communities.iter().enumerate() {\n                println!(\"Community {}: {} members\", i, comm.len());\n            }\n        },\n        Err(e) =&gt; println!(\"Error: {}\", e),\n    }\n}\n</code></pre>"},{"location":"examples/community/#using-infomap-for-flow-based-clustering","title":"Using Infomap for Flow-Based Clustering","text":"<p>Infomap is ideal when the flow of information on the network is more important than structural density.</p> <pre><code>use graphina::core::types::Graph;\nuse graphina::community::infomap::infomap;\n\nfn main() {\n    let mut graph = Graph::&lt;&amp;str, f64&gt;::new();\n    let n1 = graph.add_node(\"A\");\n    let n2 = graph.add_node(\"B\");\n    graph.add_edge(n1, n2, 1.0);\n\n    let communities = infomap(&amp;graph, 100, None).unwrap();\n    // communities is a Vec&lt;usize&gt; mapping NodeIndex -&gt; ModuleID\n}\n</code></pre>"},{"location":"examples/generators/","title":"Graph Generators Examples","text":"<p>Graph generators are essential for testing algorithms and creating benchmarks. This example demonstrates how to generate various random graphs and analyze their properties.</p> <pre><code>use graphina::core::generators::{erdos_renyi_graph, barabasi_albert_graph, watts_strogatz_graph};\nuse graphina::core::types::Undirected;\nuse graphina::metrics::{diameter, average_clustering};\n\nfn main() {\n    let seed = 42;\n    let n = 1000;\n\n    // 1. Erd\u0151s-R\u00e9nyi Graph (Random)\n    // n=1000, p=0.01\n    // Features: Low clustering, Poisson degree distribution\n    println!(\"Generating Erd\u0151s-R\u00e9nyi graph...\");\n    let er_graph = erdos_renyi_graph::&lt;Undirected&gt;(n, 0.01, seed).unwrap();\n    println!(\"  ER Edges: {}\", er_graph.edge_count());\n    println!(\"  ER Clustering: {:.4}\", average_clustering(&amp;er_graph));\n\n    // 2. Barab\u00e1si-Albert Graph (Scale-Free)\n    // n=1000, edges per new node=3\n    // Features: Hubs, Power-law degree distribution\n    println!(\"\\nGenerating Barab\u00e1si-Albert graph...\");\n    let ba_graph = barabasi_albert_graph::&lt;Undirected&gt;(n, 3, seed).unwrap();\n    println!(\"  BA Edges: {}\", ba_graph.edge_count());\n    // BA graphs typically have higher max degree due to hubs\n    let max_degree = ba_graph.nodes().map(|(id, _)| ba_graph.degree(id)).max().unwrap_or(0);\n    println!(\"  BA Max Degree: {}\", max_degree);\n\n    // 3. Watts-Strogatz Graph (Small World)\n    // n=1000, k=10 neighbors, p=0.1 rewiring\n    // Features: High clustering like regular lattice, short paths like random graph\n    println!(\"\\nGenerating Watts-Strogatz graph...\");\n    let ws_graph = watts_strogatz_graph::&lt;Undirected&gt;(n, 10, 0.1, seed).unwrap();\n    println!(\"  WS Edges: {}\", ws_graph.edge_count());\n    println!(\"  WS Clustering: {:.4}\", average_clustering(&amp;ws_graph));\n\n    // Comparison summary\n    println!(\"\\nSummary:\");\n    println!(\"Scale-free networks (BA) model social networks well.\");\n    println!(\"Small-world networks (WS) model biological and neural networks well.\");\n    println!(\"Random graphs (ER) serve as good mathematical baselines.\");\n}\n</code></pre>"},{"location":"examples/io/","title":"Graph I/O Examples","text":"<p>Persisting graphs is crucial for long-running applications. Graphina supports efficient binary formats as well as standard text formats.</p> <pre><code>use graphina::core::types::Graph;\nuse graphina::core::io::{write_edge_list, read_edge_list};\nuse graphina::core::serialization::{save_binary, load_binary, save_json, load_json};\nuse std::fs;\n\nfn main() -&gt; Result&lt;(), Box&lt;dyn std::error::Error&gt;&gt; {\n    // 1. Create a sample graph\n    let mut graph = Graph::&lt;String, f64&gt;::new();\n    let n1 = graph.add_node(\"Node A\".to_string());\n    let n2 = graph.add_node(\"Node B\".to_string());\n    let n3 = graph.add_node(\"Node C\".to_string());\n    graph.add_edge(n1, n2, 1.5);\n    graph.add_edge(n2, n3, 2.5);\n    graph.add_edge(n3, n1, 3.5);\n\n    println!(\"Original: {} nodes, {} edges\", graph.node_count(), graph.edge_count());\n\n    // 2. Binary Serialization (Fastest, Compact)\n    // Best for saving state between runs of the same application\n    save_binary(&amp;graph, \"temp_graph.bin\")?;\n    let loaded_bin: Graph&lt;String, f64&gt; = load_binary(\"temp_graph.bin\")?;\n    println!(\"Loaded Binary: {} nodes\", loaded_bin.node_count());\n\n    // 3. JSON Serialization (Human readable, Portable)\n    // Good for debugging or web APIs\n    save_json(&amp;graph, \"temp_graph.json\")?;\n    let loaded_json: Graph&lt;String, f64&gt; = load_json(\"temp_graph.json\")?;\n    println!(\"Loaded JSON: {} nodes\", loaded_json.node_count());\n\n    // 4. Edge List (Universally compatible)\n    // Good for exporting to tools like Gephi, NetworkX, Pandas\n    write_edge_list(&amp;graph, \"temp_graph.txt\", \" \")?;\n    // Note: Edge list doesn't preserve complex node attributes, usually just IDs\n    // So we read it back as integer IDs if we want structure, or parse specially.\n    // Here we just demo writing.\n\n    // Cleanup\n    fs::remove_file(\"temp_graph.bin\")?;\n    fs::remove_file(\"temp_graph.json\")?;\n    fs::remove_file(\"temp_graph.txt\")?;\n\n    Ok(())\n}\n</code></pre>"},{"location":"examples/links/","title":"Link Prediction Examples","text":"<p>Link prediction algorithms help identify missing or future connections in a network.</p>"},{"location":"examples/links/#predicting-friends-with-jaccard-coefficient","title":"Predicting Friends with Jaccard Coefficient","text":"<p>The Jaccard coefficient is a classic way to measure similarity between two sets of neighbors.</p> <pre><code>use graphina::core::types::Graph;\nuse graphina::links::similarity::jaccard_coefficient;\n\nfn main() {\n    let mut graph = Graph::&lt;&amp;str, f64&gt;::new();\n    let n0 = graph.add_node(\"Alice\");\n    let n1 = graph.add_node(\"Bob\");\n    let n2 = graph.add_node(\"Charlie\");\n    let n3 = graph.add_node(\"David\");\n\n    // Alice and Bob share Charlie as a friend\n    graph.add_edge(n0, n2, 1.0);\n    graph.add_edge(n1, n2, 1.0);\n\n    // Run prediction for all pairs\n    let predictions = jaccard_coefficient(&amp;graph, None);\n\n    for ((u, v), score) in predictions {\n        // Filter out existing edges if desired\n        if !graph.contains_edge(u, v) &amp;&amp; u != v {\n            println!(\"Score between {:?} and {:?}: {:.4}\", u, v, score);\n        }\n    }\n}\n</code></pre>"},{"location":"examples/links/#using-resource-allocation-index","title":"Using Resource Allocation Index","text":"<p>The Resource Allocation (RA) index is often more effective than Jaccard for social networks as it penalizes common neighbors that are \"hubs\" (have high degree).</p> <pre><code>use graphina::core::types::Graph;\nuse graphina::links::allocation::resource_allocation_index;\n\nfn main() {\n    let mut graph = Graph::&lt;&amp;str, f64&gt;::new();\n    let n0 = graph.add_node(\"Alice\");\n    let n1 = graph.add_node(\"Bob\");\n    let n2 = graph.add_node(\"Charlie\");\n\n    // Alice and Bob both know Charlie\n    graph.add_edge(n0, n2, 1.0);\n    graph.add_edge(n1, n2, 1.0);\n\n    let ra_scores = resource_allocation_index(&amp;graph, None);\n    // Process scores...\n}\n</code></pre>"},{"location":"examples/links/#community-aware-prediction","title":"Community-Aware Prediction","text":"<p>If you have community labels, you can use Soundarajan-Hopcroft variants to boost scores for nodes in the same community.</p> <pre><code>use graphina::core::types::Graph;\nuse graphina::links::allocation::ra_index_soundarajan_hopcroft;\n\nfn main() {\n    let mut graph = Graph::&lt;&amp;str, f64&gt;::new();\n    let n1 = graph.add_node(\"A\");\n    let n2 = graph.add_node(\"B\");\n\n    // Two nodes in the same community (e.g., both even indices)\n    let n3 = graph.add_node(\"C\");\n    graph.add_edge(n1, n2, 1.0);\n    graph.add_edge(n2, n3, 1.0);\n\n    // Mock community assignment closure\n    let community_map = |node_id| {\n        // In reality, lookup from a HashMap or property\n        if node_id.index() % 2 == 0 { 1 } else { 2 }\n    };\n\n    let scores = ra_index_soundarajan_hopcroft(&amp;graph, None, community_map);\n}\n</code></pre>"},{"location":"examples/metrics/","title":"Graph Metrics Examples","text":"<p>Compute structural properties of your graph.</p>"},{"location":"examples/metrics/#distance-metrics","title":"Distance Metrics","text":"<p>These metrics help you understand the size and connectivity of the graph.</p> <pre><code>use graphina::core::types::Graph;\nuse graphina::metrics::{diameter, radius, average_path_length};\n\nfn main() {\n    let mut graph = Graph::&lt;&amp;str, f64&gt;::new();\n    let n1 = graph.add_node(\"A\");\n    let n2 = graph.add_node(\"B\");\n    let n3 = graph.add_node(\"C\");\n    let n4 = graph.add_node(\"D\");\n\n    graph.add_edge(n1, n2, 1.0);\n    graph.add_edge(n2, n3, 1.0);\n    graph.add_edge(n3, n4, 1.0);\n\n    if let Some(d) = diameter(&amp;graph) {\n        println!(\"Diameter: {}\", d);\n    }\n\n    if let Some(r) = radius(&amp;graph) {\n        println!(\"Radius: {}\", r);\n    }\n\n    // Average steps between any two nodes\n    let avg = average_path_length(&amp;graph);\n    println!(\"Average Path Length: {:.2}\", avg);\n}\n</code></pre>"},{"location":"examples/metrics/#clustering-and-mixing","title":"Clustering and Mixing","text":"<p>Understanding how nodes cluster together.</p> <pre><code>use graphina::core::types::Graph;\nuse graphina::metrics::{\n    average_clustering_coefficient,\n    transitivity,\n    assortativity\n};\n\nfn main() {\n    let mut graph = Graph::&lt;&amp;str, f64&gt;::new();\n    let n1 = graph.add_node(\"A\");\n    let n2 = graph.add_node(\"B\");\n    let n3 = graph.add_node(\"C\");\n\n    graph.add_edge(n1, n2, 1.0);\n    graph.add_edge(n2, n3, 1.0);\n    graph.add_edge(n3, n1, 1.0);\n\n    // Local clustering averaged over all nodes\n    let avg_cc = average_clustering_coefficient(&amp;graph);\n    println!(\"Avg Clustering Coeff: {:.4}\", avg_cc);\n\n    // Global clustering (triangles / connected triples)\n    let trans = transitivity(&amp;graph);\n    println!(\"Transitivity: {:.4}\", trans);\n\n    // Assortativity (do high-degree nodes connect to high-degree nodes?)\n    let r = assortativity(&amp;graph);\n    println!(\"Assortativity: {:.4}\", r);\n}\n</code></pre>"},{"location":"examples/mst/","title":"Minimum Spanning Tree Examples","text":"<p>Find the subset of edges that connects the graph with minimal total weight.</p>"},{"location":"examples/mst/#prims-algorithm","title":"Prim's Algorithm","text":"<p>Use Prim's for dense graphs or when you want to grow a tree from a specific node.</p> <pre><code>use graphina::core::types::Graph;\nuse graphina::mst::prim_mst;\nuse ordered_float::OrderedFloat;\n\nfn main() {\n    // Note: Weights must be Ord (OrderedFloat for f64)\n    let mut graph = Graph::&lt;&amp;str, OrderedFloat&lt;f64&gt;&gt;::new();\n\n    let n1 = graph.add_node(\"A\");\n    let n2 = graph.add_node(\"B\");\n    let n3 = graph.add_node(\"C\");\n\n    graph.add_edge(n1, n2, OrderedFloat(1.0));\n    graph.add_edge(n2, n3, OrderedFloat(2.0));\n    graph.add_edge(n1, n3, OrderedFloat(10.0));\n\n    // Returns (Vec&lt;(u, v, w)&gt;, total_weight)\n    if let Ok((edges, total)) = prim_mst(&amp;graph) {\n        println!(\"MST Total Weight: {}\", total);\n        for (u, v, w) in edges {\n            println!(\"Edge: {:?} - {:?} (wt: {})\", u, v, w);\n        }\n    }\n}\n</code></pre>"},{"location":"examples/mst/#kruskals-algorithm","title":"Kruskal's Algorithm","text":"<p>Kruskal's is efficient for sparse graphs and works by sorting edges.</p> <pre><code>use graphina::core::types::Graph;\nuse graphina::mst::kruskal_mst;\nuse ordered_float::OrderedFloat;\n\nfn main() {\n    let mut graph = Graph::&lt;&amp;str, OrderedFloat&lt;f64&gt;&gt;::new();\n    let n1 = graph.add_node(\"A\");\n    let n2 = graph.add_node(\"B\");\n    graph.add_edge(n1, n2, OrderedFloat(1.0));\n\n    if let Ok((edges, total)) = kruskal_mst(&amp;graph) {\n        println!(\"Kruskal MST Weight: {}\", total);\n    }\n}\n</code></pre>"},{"location":"examples/parallel/","title":"Parallel Processing Examples","text":"<p>Leverage multi-core CPUs for large graph analysis using Rayon.</p> <p>Parallel Feature</p> <p>Make sure the <code>parallel</code> feature is enabled in your <code>Cargo.toml</code>.</p>"},{"location":"examples/parallel/#parallel-pagerank","title":"Parallel PageRank","text":"<p>Calculates PageRank significantly faster on large graphs.</p> <pre><code>use graphina::core::types::Digraph;\nuse graphina::parallel::pagerank_parallel;\n\nfn main() {\n    let mut graph = Digraph::&lt;&amp;str, f64&gt;::new();\n\n    // Imagine adding 100,000 nodes and edges...\n    // Create a larger graph\n    for i in 0..100 {\n        let n = graph.add_node(\"Node\");\n        if i &gt; 0 {\n            // Connect to previous to form a chain/line\n            // In a real scenario, you'd add many edges\n        }\n    }\n\n    // Arguments: graph, damping, iteration, tolerance, nstart\n    match pagerank_parallel(&amp;graph, 0.85, 100, 1e-6, None) {\n        Ok(scores) =&gt; {\n            println!(\"Computed {} ranks.\", scores.len());\n        },\n        Err(e) =&gt; println!(\"Error: {}\", e),\n    }\n}\n</code></pre>"},{"location":"examples/parallel/#parallel-bfs","title":"Parallel BFS","text":"<p>Run BFS from multiple source nodes simultaneously. Useful for distance estimations or analyzing spread.</p> <pre><code>use graphina::core::types::Digraph;\nuse graphina::parallel::bfs::bfs_parallel;\n\nfn main() {\n    let mut graph = Digraph::&lt;&amp;str, f64&gt;::new();\n    let n1 = graph.add_node(\"A\");\n    let n2 = graph.add_node(\"B\");\n    graph.add_edge(n1, n2, 1.0);\n\n    let start_nodes = vec![n1];\n\n    // visited is a HashSet of all nodes reachable from ANY start node\n    let visited = bfs_parallel(&amp;graph, start_nodes);\n    println!(\"Total reachable nodes: {}\", visited.len());\n}\n</code></pre>"},{"location":"examples/paths/","title":"Path Finding Examples","text":"<p>Examples of finding shortest paths in Graphina.</p>"},{"location":"examples/paths/#dijkstras-algorithm","title":"Dijkstra's Algorithm","text":""},{"location":"examples/paths/#basic-usage-with-f64-weights","title":"Basic Usage with <code>f64</code> Weights","text":"<pre><code>use graphina::core::types::Graph;\nuse graphina::core::paths::dijkstra_path_f64;\n\nfn main() {\n    let mut graph = Graph::new();\n    let ids = (0..5).map(|i| graph.add_node(i)).collect::&lt;Vec&lt;_&gt;&gt;();\n\n    // Add edges: (source, target, weight)\n    let edges = [(0, 1, 1.0), (1, 2, 1.0), (2, 3, 2.0), (3, 4, 1.0)];\n    for (s, d, w) in edges {\n        graph.add_edge(ids[s], ids[d], w);\n    }\n\n    // Find path from node 0\n    let (cost, trace) = dijkstra_path_f64(&amp;graph, ids[0], None).unwrap();\n\n    println!(\"Distance to node 4: {:?}\", cost[&amp;ids[4]]);\n}\n</code></pre>"},{"location":"examples/paths/#complex-example-flight-network","title":"Complex Example: Flight Network","text":"<p>This example demonstrates using custom edge types and a custom cost evaluation function. Imagine a flight network where edges have both a price and an aircraft type.</p> <pre><code>use graphina::core::types::Digraph;\nuse graphina::core::paths::dijkstra_path_impl;\n\nfn main() {\n    // Edge stores (Price, Aircraft Type)\n    let mut graph: Digraph&lt;String, (f64, String)&gt; = Digraph::new();\n\n    let cities = [\"ATL\", \"PEK\", \"LHR\", \"HND\", \"CDG\", \"FRA\", \"HKG\"];\n    let ids: Vec&lt;_&gt; = cities.iter().map(|s| graph.add_node(s.to_string())).collect();\n\n    // Define flights\n    // (Source, Dest, (Price, Aircraft))\n    let flights = [\n        (\"ATL\", \"PEK\", (900.0, \"boeing\")),\n        (\"ATL\", \"LHR\", (500.0, \"airbus\")),\n        (\"PEK\", \"LHR\", (800.0, \"boeing\")),\n        (\"LHR\", \"FRA\", (200.0, \"boeing\")),\n    ];\n\n    // Build graph\n    for (source, dest, (price, model)) in flights {\n        // Find NodeIds (in a real app, use a HashMap to look them up efficiently)\n        let s_id = cities.iter().position(|&amp;c| c == source).unwrap();\n        let d_id = cities.iter().position(|&amp;c| c == dest).unwrap();\n\n        // Add edge with (Price, Aircraft)\n        graph.add_edge(ids[s_id], ids[d_id], (price, model.to_string()));\n    }\n\n    // Custom Cost Function: Avoid Boeing planes!\n    // Returns Some(cost) if passable, None if impassable.\n    let eval_cost = |(price, manufacturer): &amp;(f64, String)| match manufacturer.as_str() {\n        \"boeing\" =&gt; None,         // \"I'm not flying Boeing!\"\n        _ =&gt; Some(*price),        // \"Airbus is fine.\"\n    };\n\n    // Run Dijkstra with custom evaluator\n    // cutoff: Some(1000.0) -&gt; Stop if cost exceeds 1000\n    let (cost, _trace) = dijkstra_path_impl(&amp;graph, ids[0], Some(1000.0), eval_cost).unwrap();\n\n    // Result will ignore paths using Boeing planes\n}\n</code></pre>"},{"location":"examples/subgraphs/","title":"Subgraphs and Filtering Examples","text":"<p>Extract specific parts of your graph for focused analysis.</p>"},{"location":"examples/subgraphs/#extracting-an-induced-subgraph","title":"Extracting an Induced Subgraph","text":"<p>Create a new graph containing only a specific set of nodes and their connections.</p> <pre><code>use graphina::core::types::Graph;\nuse graphina::subgraphs::SubgraphOps;\n\nfn main() {\n    let mut graph = Graph::&lt;&amp;str, f64&gt;::new();\n    let n1 = graph.add_node(\"A\");\n    let n2 = graph.add_node(\"B\");\n    let n3 = graph.add_node(\"C\");\n    graph.add_edge(n1, n2, 1.0);\n    graph.add_edge(n2, n3, 1.0);\n\n    let subset = vec![n1, n2];\n\n    // New graph contains only A and B, and the edge A-B\n    let subgraph = graph.subgraph(&amp;subset).unwrap();\n    println!(\"Subgraph nodes: {}\", subgraph.node_count());\n}\n</code></pre>"},{"location":"examples/subgraphs/#identifying-neighborhoods-ego-graph","title":"Identifying Neighborhoods (Ego Graph)","text":"<p>Analyze the immediate surroundings of a node <code>k</code> hops away.</p> <pre><code>use graphina::core::types::Graph;\nuse graphina::subgraphs::SubgraphOps;\n\nfn main() {\n    let mut graph = Graph::&lt;&amp;str, f64&gt;::new();\n    let center = graph.add_node(\"Center\");\n    let leaf1 = graph.add_node(\"L1\");\n    let leaf2 = graph.add_node(\"L2\");\n\n    graph.add_edge(center, leaf1, 1.0);\n    graph.add_edge(center, leaf2, 1.0);\n\n    // All nodes within 1 hop of \"Center\" (radius was corrected to 1 for this small example)\n    let ego = graph.ego_graph(center, 1).unwrap();\n    println!(\"Ego network size: {}\", ego.node_count());\n}\n</code></pre>"},{"location":"examples/subgraphs/#filtering-by-attribute","title":"Filtering by Attribute","text":"<p>Keep only nodes or edges that match a predicate.</p> <pre><code>use graphina::core::types::Graph;\nuse graphina::subgraphs::SubgraphOps;\n\nfn main() {\n    let mut graph = Graph::&lt;i32, f64&gt;::new();\n    // Add nodes 0 to 9\n    for i in 0..10 {\n        graph.add_node(i);\n    }\n\n    // Filter: Keep only even numbers\n    let even_graph = graph.filter_nodes(|_id, &amp;val| val % 2 == 0);\n\n    // Filter: Keep edges with weight &gt; 0.5\n    let strong_edges = graph.filter_edges(|_u, _v, w| *w &gt; 0.5);\n}\n</code></pre>"},{"location":"examples/traversal/","title":"Graph Traversal Examples","text":"<p>Walk through the graph to find paths or visit nodes.</p>"},{"location":"examples/traversal/#breadth-first-search-bfs","title":"Breadth-First Search (BFS)","text":"<p>Visit neighbors layer by layer. Good for finding shortest paths in unweighted graphs.</p> <pre><code>use graphina::core::types::Graph;\nuse graphina::traversal::bfs;\n\nfn main() {\n    let mut graph = Graph::&lt;&amp;str, f64&gt;::new();\n    let start = graph.add_node(\"Start\");\n    let end = graph.add_node(\"End\");\n\n    graph.add_edge(start, end, 1.0);\n\n    // Returns a Vec&lt;NodeId&gt; in visitation order\n    let visited_order = bfs(&amp;graph, start);\n    println!(\"Visited {} nodes via BFS\", visited_order.len());\n}\n</code></pre>"},{"location":"examples/traversal/#depth-first-search-dfs","title":"Depth-First Search (DFS)","text":"<p>Explore as deep as possible before backtracking.</p> <pre><code>use graphina::core::types::Graph;\nuse graphina::traversal::dfs;\n\nfn main() {\n    let mut graph = Graph::&lt;&amp;str, f64&gt;::new();\n    let start = graph.add_node(\"Start\");\n\n    let visited_order = dfs(&amp;graph, start);\n}\n</code></pre>"},{"location":"examples/traversal/#bidirectional-search","title":"Bidirectional Search","text":"<p>Find the shortest path between two nodes by searching from both ends simultaneously.</p> <pre><code>use graphina::core::types::Graph;\nuse graphina::traversal::bidis;\n\nfn main() {\n    let mut graph = Graph::&lt;&amp;str, f64&gt;::new();\n    let start = graph.add_node(\"Start\");\n    let end = graph.add_node(\"End\");\n\n    // Connect them\n    let mid = graph.add_node(\"Mid\");\n    graph.add_edge(start, mid, 1.0);\n    graph.add_edge(mid, end, 1.0);\n\n    if let Some(path) = bidis(&amp;graph, start, end) {\n        println!(\"Shortest path length (hops): {}\", path.len() - 1);\n        println!(\"Path: {:?}\", path);\n    } else {\n        println!(\"No path found.\");\n    }\n}\n</code></pre>"},{"location":"examples/visualization/","title":"Visualization Examples","text":""},{"location":"examples/visualization/#quick-ascii-visualization","title":"Quick ASCII Visualization","text":"<p>Useful for debugging small graphs directly in the terminal.</p> <pre><code>use graphina::core::types::Graph;\n\nfn main() {\n    let mut graph = Graph::&lt;&amp;str, f64&gt;::new();\n    let a = graph.add_node(\"Alice\");\n    let b = graph.add_node(\"Bob\");\n    graph.add_edge(a, b, 1.0);\n\n    println!(\"{}\", graph.to_ascii_art());\n}\n</code></pre> <p>Output: <pre><code>Nodes:\n  [0] Alice (degree: 1)\n  [1] Bob (degree: 1)\n\nEdges:\n  [0] -- [1] (weight: 1)\n</code></pre></p>"},{"location":"examples/visualization/#interactive-html-export","title":"Interactive HTML Export","text":"<p>Generate a standalone HTML file with a force-directed layout.</p> <pre><code>use graphina::visualization::{LayoutAlgorithm, VisualizationConfig};\nuse graphina::core::types::Graph;\n\nfn main() {\n    let mut graph = Graph::&lt;&amp;str, f64&gt;::new();\n    let a = graph.add_node(\"A\");\n    let b = graph.add_node(\"B\");\n    graph.add_edge(a, b, 1.0);\n\n    let mut config = VisualizationConfig::default();\n    config.layout = LayoutAlgorithm::ForceDirected;\n    config.node_color = \"#4CAF50\".to_string(); // Custom Green\n    config.edge_color = \"#2196F3\".to_string(); // Custom Blue\n\n    // Creates 'graph.html' which you can open in a browser\n    graph.save_as_html(\"graph.html\", &amp;config).unwrap();\n}\n</code></pre>"},{"location":"examples/visualization/#static-image-export-png-svg","title":"Static Image Export (PNG / SVG)","text":"<p>Requires the <code>visualization</code> feature enabled.</p> <pre><code>use graphina::visualization::{LayoutAlgorithm, VisualizationConfig};\nuse graphina::core::types::Graph;\n\nfn main() {\n    // Setup graph\n    let mut graph = Graph::&lt;&amp;str, f64&gt;::new();\n    let n1 = graph.add_node(\"1\");\n    let n2 = graph.add_node(\"2\");\n    graph.add_edge(n1, n2, 1.0);\n\n    // Force-directed PNG\n    let mut config = VisualizationConfig::default();\n    config.width = 1000;\n    config.height = 800;\n    graph.save_as_png(\"graph_force.png\", &amp;config).unwrap();\n\n    // Circular SVG\n    config.layout = LayoutAlgorithm::Circular;\n    graph.save_as_svg(\"graph_circular.svg\", &amp;config).unwrap();\n}\n</code></pre>"},{"location":"examples/visualization/#d3js-json-export","title":"D3.js JSON Export","text":"<p>Export raw data to build custom frontends.</p> <pre><code>use graphina::core::types::Graph;\n\nfn main() {\n    let mut graph = Graph::&lt;&amp;str, f64&gt;::new();\n    let n = graph.add_node(\"Node\");\n\n    let json_str = graph.to_d3_json().unwrap();\n    std::fs::write(\"data.json\", json_str).unwrap();\n}\n</code></pre>"},{"location":"getting-started/concepts/","title":"Core Concepts in Graphina","text":"<p>Graphina is designed to be fast, memory-efficient, and easy to use, but coming from dynamic libraries like NetworkX, some concepts might be new.</p>"},{"location":"getting-started/concepts/#graph-types","title":"Graph Types","text":"<p>Graphina provides two primary graph structures, both of which are generic over node attributes (<code>A</code>) and edge weights (<code>W</code>).</p> <ul> <li><code>Graph&lt;A, W&gt;</code>: An undirected graph. Edges have no direction; <code>add_edge(a, b, w)</code> effectively connects \\(a \\leftrightarrow b\\).</li> <li><code>Digraph&lt;A, W&gt;</code>: A directed graph. Edges go from source to target. <code>neighbors(u)</code> returns only outgoing neighbors (successors).</li> </ul>"},{"location":"getting-started/concepts/#strongly-typed-data","title":"Strongly Typed Data","text":"<p>Unlike Python where a graph can hold mixed types (like strings, ints, objects), Graphina graphs are strongly typed.</p> <pre><code>// A social network: Nodes are people (String), Edges are relationship strength (f64)\nlet g = Graph::&lt;String, f64&gt;::new();\n\n// A road map: Nodes are intersections (Point struct), Edges are distances (u32)\nlet g = Graph::&lt;Point, u32&gt;::new();\n</code></pre> <p>This design allows Graphina to optimize memory layout and guarantee data consistency at compile time.</p>"},{"location":"getting-started/concepts/#references-via-nodeid","title":"References via NodeId","text":"<p>In NetworkX, the node value (like \"Alice\") is often the identifier. In Graphina, adding a node transfers ownership of the data to the graph and returns a <code>NodeId</code>.</p> <pre><code>let id = graph.add_node(\"Data\");\n</code></pre> <p>This <code>NodeId</code> is a lightweight handle (essentially an integer index) that permits \\(O(1)\\) access to the node. You use this ID for all subsequent graph operations:</p> <ul> <li>Creating edges (<code>graph.add_edge(id1, id2, ...)</code>)</li> <li>Querying neighbors (<code>graph.neighbors(id1)</code>)</li> <li>Algorithms (<code>pagerank(&amp;graph, ...)</code>)</li> </ul>"},{"location":"getting-started/concepts/#the-try_-api-vs-panicking-api","title":"The <code>try_</code> API vs Panicking API","text":"<p>Graphina offers two styles of interaction:</p> <p>Use the <code>try_</code> variants or <code>Option/Result</code> returning methods for robustness:</p> <ul> <li><code>graph.add_node(...)</code> -&gt; matches <code>NetworkX</code> behavior (panics or updates).</li> <li><code>graph.try_add_node(...)</code> -&gt; Returns <code>Result.&lt;_, GraphinaError&gt;</code>. Recommended for production applications where you need to handle missing nodes or invalid operations gracefully.</li> </ul>"},{"location":"getting-started/concepts/#performance","title":"Performance","text":"<p>Graphina wraps <code>petgraph</code>'s <code>StableGraph</code>, meaning:</p> <ul> <li>Fast Lookups: Nodes and edges are stored in vectors.</li> <li>Stability: Removing a node does not invalidate the <code>NodeId</code>s of other nodes.</li> </ul>"},{"location":"getting-started/installation/","title":"Installation","text":"<p>To add <code>graphina</code> to your project, run:</p> <pre><code>cargo add graphina\n</code></pre> <p>Or manually add it to your <code>Cargo.toml</code>:</p> <pre><code>[dependencies]\ngraphina = \"0.3.0-alpha.4\"\n</code></pre>"},{"location":"getting-started/installation/#feature-flags","title":"Feature Flags","text":"<p>Graphina is modular. Enable only what you need using the respective feature flags.</p> <ul> <li><code>centrality</code>: Centrality measures (PageRank, Betweenness, etc.)</li> <li><code>community</code>: Community detection (Louvain, Label Propagation, etc.)</li> <li><code>links</code>: Link prediction algorithms (like Jaccard and Adamic-Adar indexes)</li> <li><code>approximation</code>: Approximation algorithms for NP-hard problems</li> <li><code>metrics</code>: Graph metrics (like Diameter, Clustering Coefficient)</li> <li><code>mst</code>: Minimum Spanning Tree algorithms</li> <li><code>traversal</code>: Graph traversal algorithms (BFS and DFS)</li> <li><code>subgraphs</code>: Subgraph extraction and filtering</li> <li><code>parallel</code>: Parallel algorithm implementations</li> <li><code>visualization</code>: Visualization helper functions</li> </ul> <pre><code>[dependencies]\ngraphina = { version = \"0.3.0-alpha.4\", features = [\"centrality\", \"parallel\"] }\n</code></pre> <p>You can also add features via command line:</p> <pre><code>cargo add graphina --features centrality,parallel\n</code></pre>"},{"location":"getting-started/quickstart/","title":"Quick Start with Graphina","text":"<p>Create graphs, add nodes, and run algorithms with Graphina.</p>"},{"location":"getting-started/quickstart/#creating-a-graph","title":"Creating a Graph","text":"<p>Rust requires defined types for node and edge data. Create an undirected graph with <code>String</code> nodes and <code>f64</code> edge weights:</p> <pre><code>use graphina::core::types::Graph;\n\nfn main() {\n    // Node type: String, Edge type: f64\n    let mut graph = Graph::&lt;String, f64&gt;::new();\n}\n</code></pre> <p>Graph Type</p> <p>Use <code>Digraph</code> for directed graphs.</p>"},{"location":"getting-started/quickstart/#adding-nodes","title":"Adding Nodes","text":"<p><code>add_node</code> returns a <code>NodeId</code>. Use this ID to reference the node, like when adding edges.</p> <pre><code>    // Add nodes\n    let n1 = graph.add_node(\"Alice\".to_string());\n    let n2 = graph.add_node(\"Bob\".to_string());\n    let n3 = graph.add_node(\"Charlie\".to_string());\n\n    println!(\"Graph has {} nodes\", graph.node_count());\n</code></pre>"},{"location":"getting-started/quickstart/#adding-edges","title":"Adding Edges","text":"<p>Connect nodes using their <code>NodeId</code>s.</p> <pre><code>    // Add weighted edges\n    graph.add_edge(n1, n2, 1.0); // Alice -- Bob\n    graph.add_edge(n2, n3, 2.0); // Bob -- Charlie\n\n    // Add an edge and get its ID\n    let e1 = graph.add_edge(n1, n3, 0.5); // Alice -- Charlie\n\n    println!(\"Graph has {} edges\", graph.edge_count());\n</code></pre>"},{"location":"getting-started/quickstart/#examining-the-graph","title":"Examining the Graph","text":"<p>Check for existence, degrees, or neighbors.</p> <pre><code>    // Check degree\n    let degree = graph.degree(n2);\n    println!(\"Bob's degree: {:?}\", degree); // Some(2)\n\n    // Iterate over neighbors\n    println!(\"Alice's neighbors:\");\n    for neighbor_id in graph.neighbors(n1) {\n        // Access the node data using the index operator\n        println!(\"- {}\", graph[neighbor_id]);\n    }\n</code></pre>"},{"location":"getting-started/quickstart/#running-algorithms","title":"Running Algorithms","text":"<p>Graphina provides standard algorithms in modules like <code>centrality</code>, <code>community</code>, and <code>paths</code>.</p> <p>Calculate PageRank:</p> <pre><code>use graphina::centrality::pagerank;\n\n    // Create a simple graph for the example\n    let mut graph = Graph::&lt;String, f64&gt;::new();\n    let n1 = graph.add_node(\"Alice\".to_string());\n    let n2 = graph.add_node(\"Bob\".to_string());\n    graph.add_edge(n1, n2, 1.0);\n\n    // Calculate PageRank\n    // (graph, damping_factor, max_iterations, tolerance, nstart)\n    let scores = pagerank(&amp;graph, 0.85, 100, 1e-6, None).unwrap();\n\n    println!(\"PageRank scores:\");\n    for (node_id, score) in scores {\n        println!(\"{}: {:.4}\", graph[node_id], score);\n    }\n</code></pre>"},{"location":"getting-started/quickstart/#complete-example","title":"Complete Example","text":"<p>Combine creation, population, and analysis:</p> <pre><code>use graphina::core::types::Graph;\nuse graphina::centrality::pagerank;\n\nfn main() {\n    let mut g = Graph::&lt;&amp;str, f64&gt;::new();\n\n    let a = g.add_node(\"A\");\n    let b = g.add_node(\"B\");\n    let c = g.add_node(\"C\");\n\n    g.add_edge(a, b, 1.0);\n    g.add_edge(b, c, 1.0);\n    g.add_edge(c, a, 1.0);\n\n    println!(\"Nodes: {}, Edges: {}\", g.node_count(), g.edge_count());\n\n    let ranks = pagerank(&amp;g, 0.85, 100, 1e-6, None).unwrap();\n    println!(\"PageRank for A: {:.4}\", ranks.get(&amp;a).unwrap());\n}\n</code></pre>"},{"location":"getting-started/quickstart/#advanced-graph-creation","title":"Advanced Graph Creation","text":"<p>For more control over graph properties (capacity, self-loops, etc.), use the <code>AdvancedGraphBuilder</code>.</p> <pre><code>use graphina::core::builders::UndirectedGraphBuilder;\n\nfn main() {\n    let graph = UndirectedGraphBuilder::&lt;i32, f64&gt;::undirected()\n        .with_capacity(100, 200)       // Pre-allocate memory\n        .allow_self_loops(false)       // Disallow self-loops\n        .allow_parallel_edges(false)   // Disallow parallel edges\n        .add_node(1)\n        .add_node(2)\n        .add_edge(0, 1, 1.0)\n        .build()\n        .unwrap();\n\n    println!(\"Graph created with {} nodes\", graph.node_count());\n}\n</code></pre>"},{"location":"guide/approximation/","title":"Approximation Algorithms","text":"<p>Graphina provides approximation algorithms for several NP-hard graph problems.</p>"},{"location":"guide/approximation/#traveling-salesman-problem-tsp","title":"Traveling Salesman Problem (TSP)","text":"<p>Finds an approximate solution to the TSP (shortest tour visiting all nodes).</p>"},{"location":"guide/approximation/#greedy-algorithm","title":"Greedy Algorithm","text":"<p>Constructs a tour by repeatedly visiting the nearest unvisited node.</p> <pre><code>use graphina::approximation::tsp::greedy_tsp;\nuse ordered_float::OrderedFloat;\n\n// Weights must implementation Ord (use OrderedFloat for f64)\nlet g_ord = graph.convert::&lt;OrderedFloat&lt;f64&gt;&gt;();\nif let Ok((tour, cost)) = greedy_tsp(&amp;g_ord, start_node) {\n    println!(\"Tour: {:?}, Cost: {}\", tour, cost);\n}\n</code></pre>"},{"location":"guide/approximation/#vertex-cover","title":"Vertex Cover","text":"<p>Finds a subset of nodes such that every edge in the graph is incident to at least one node in the subset.</p> <pre><code>use graphina::approximation::vertex_cover::min_vertex_cover;\n\nlet cover = min_vertex_cover(&amp;graph);\n</code></pre>"},{"location":"guide/approximation/#maximum-independent-set","title":"Maximum Independent Set","text":"<p>Finds a set of nodes where no two nodes in the set are adjacent.</p> <pre><code>use graphina::approximation::independent_set::max_independent_set;\n\nlet set = max_independent_set(&amp;graph);\n</code></pre>"},{"location":"guide/approximation/#maximum-clique","title":"Maximum Clique","text":"<p>Finds the largest subset of nodes where every node is connected to every other node in the subset.</p> <pre><code>use graphina::approximation::clique::max_clique;\n\nlet clique = max_clique(&amp;graph);\n</code></pre>"},{"location":"guide/approximation/#other-approximations","title":"Other Approximations","text":""},{"location":"guide/approximation/#average-clustering-coefficient","title":"Average Clustering Coefficient","text":"<p>Estimates the average local clustering coefficient.</p> <pre><code>use graphina::approximation::clustering::average_clustering;\n\nlet avg_cc = average_clustering(&amp;graph);\n</code></pre>"},{"location":"guide/approximation/#local-node-connectivity","title":"Local Node Connectivity","text":"<p>Approximates the local node connectivity between two nodes using repeated BFS.</p> <pre><code>use graphina::approximation::connectivity::local_node_connectivity;\n\nlet conn = local_node_connectivity(&amp;graph, source, target);\n</code></pre>"},{"location":"guide/approximation/#diameter-lower-bound","title":"Diameter Lower Bound","text":"<p>Approximates the diameter of the graph by sampling.</p> <pre><code>use graphina::approximation::diameter::approximate_diameter;\n\nlet diameter = approximate_diameter(&amp;graph).unwrap();\n</code></pre>"},{"location":"guide/approximation/#minimum-maximal-matching","title":"Minimum Maximal Matching","text":"<p>Greedy approximation for minimum maximal matching.</p> <pre><code>use graphina::approximation::matching::min_maximal_matching;\n\nlet matching = min_maximal_matching(&amp;graph);\n</code></pre>"},{"location":"guide/approximation/#ramsey-r2-t","title":"Ramsey R(2, t)","text":"<p>Approximates the Ramsey number R(2, t) by finding a max clique and max independent set.</p> <pre><code>use graphina::approximation::ramsey::ramsey_r2;\n\nlet (clique, ind_set) = ramsey_r2(&amp;graph);\n</code></pre>"},{"location":"guide/approximation/#densest-subgraph","title":"Densest Subgraph","text":"<p>Finds a subgraph with maximum average degree using a greedy peeling strategy.</p> <pre><code>use graphina::approximation::subgraph::densest_subgraph;\n\nlet nodes = densest_subgraph(&amp;graph, None);\n</code></pre>"},{"location":"guide/approximation/#treewidth","title":"Treewidth","text":"<p>Approximates treewidth using minimum degree or minimum fill-in heuristics.</p> <pre><code>use graphina::approximation::treewidth::{treewidth_min_degree, treewidth_min_fill_in};\n\nlet (tw, order) = treewidth_min_degree(&amp;graph);\n// or\nlet (tw, order) = treewidth_min_fill_in(&amp;graph);\n</code></pre>"},{"location":"guide/builders/","title":"Graph Builders","text":"<p>Graphina provides builder patterns to construct graphs programmatically with validation and preset configurations.</p>"},{"location":"guide/builders/#advancedgraphbuilder","title":"AdvancedGraphBuilder","text":"<p>For more control over graph properties (capacity, self-loops, etc.), use the <code>AdvancedGraphBuilder</code>.</p>"},{"location":"guide/builders/#basic-usage","title":"Basic Usage","text":"<pre><code>use graphina::core::builders::UndirectedGraphBuilder;\n\nfn main() {\n    let graph = UndirectedGraphBuilder::&lt;i32, f64&gt;::undirected()\n        .with_capacity(100, 200)       // Pre-allocate memory for nodes and edges\n        .allow_self_loops(false)       // Disallow self-loops\n        .allow_parallel_edges(false)   // Disallow parallel edges\n        .add_node(1)\n        .add_node(2)\n        .add_edge(0, 1, 1.0)\n        .build()\n        .unwrap();\n\n    println!(\"Graph created with {} nodes\", graph.node_count());\n}\n</code></pre>"},{"location":"guide/builders/#type-aliases","title":"Type Aliases","text":"<ul> <li><code>DirectedGraphBuilder</code>: Alias for <code>AdvancedGraphBuilder&lt;A, W, Directed&gt;</code></li> <li><code>UndirectedGraphBuilder</code>: Alias for <code>AdvancedGraphBuilder&lt;A, W, Undirected&gt;</code></li> </ul>"},{"location":"guide/builders/#validation","title":"Validation","text":"<p>The <code>build()</code> method validates the configuration:</p> <ul> <li>Checks if edge indices are within bounds.</li> <li>Enforces <code>allow_self_loops</code> and <code>allow_parallel_edges</code> constraints.</li> </ul>"},{"location":"guide/builders/#topologybuilder","title":"TopologyBuilder","text":"<p>The <code>TopologyBuilder</code> provides convenience methods for creating standard graph structures.</p>"},{"location":"guide/builders/#supported-topologies","title":"Supported Topologies","text":"<ul> <li>Complete Graph: Every node is connected to every other node.</li> <li>Cycle Graph: Nodes connected in a closed loop.</li> <li>Path Graph: Nodes connected in a linear chain.</li> <li>Star Graph: One central node connected to all other peripheral nodes.</li> <li>Grid Graph: Nodes arranged in a grid lattice.</li> </ul>"},{"location":"guide/builders/#examples","title":"Examples","text":"<pre><code>use graphina::core::builders::TopologyBuilder;\n\n// Create a complete graph with 5 nodes\n// Node attributes are () (unit), Edge weights are 1.0\nlet complete = TopologyBuilder::complete(5, (), 1.0);\n\n// Create a cycle graph with 6 nodes\nlet cycle = TopologyBuilder::cycle(6, \"Node\", 1.0);\n\n// Create a star graph with 10 nodes (1 center + 9 leaves)\nlet star = TopologyBuilder::star(10, (), 1.0);\n\n// Create a 3x4 grid graph\nlet grid = TopologyBuilder::grid(3, 4, (), 1.0);\n</code></pre>"},{"location":"guide/centrality/","title":"Centrality Algorithms","text":"<p>Centrality measures identify the most important nodes in a graph. Graphina provides high-performance implementations of standard centrality metrics.</p>"},{"location":"guide/centrality/#pagerank","title":"PageRank","text":"<p>PageRank computes the importance of nodes based on incoming links, modeling a random surfer.</p>"},{"location":"guide/centrality/#function-signature","title":"Function Signature","text":"<pre><code>pub fn pagerank&lt;A, W, Ty&gt;(\n    graph: &amp;BaseGraph&lt;A, W, Ty&gt;,\n    damping: f64,\n    max_iter: usize,\n    tolerance: f64,\n    nstart: Option&lt;&amp;NodeMap&lt;f64&gt;&gt;,\n) -&gt; Result&lt;NodeMap&lt;f64&gt;&gt;\n</code></pre>"},{"location":"guide/centrality/#example","title":"Example","text":"<pre><code>use graphina::core::types::Digraph;\nuse graphina::centrality::pagerank;\nuse graphina::centrality::degree_centrality; // Added for degree_centrality\nuse graphina::centrality::DegreeDirection; // Added for DegreeDirection\n\nlet mut g = Digraph::&lt;&amp;str, f64&gt;::new();\nlet n1 = g.add_node(\"A\");\nlet n2 = g.add_node(\"B\");\nlet n3 = g.add_node(\"C\");\nlet n4 = g.add_node(\"D\");\n\ng.add_edge(n1, n2, 1.0);\ng.add_edge(n1, n3, 1.0);\ng.add_edge(n2, n3, 1.0);\ng.add_edge(n3, n4, 1.0);\ng.add_edge(n4, n1, 1.0);\n\nlet scores = pagerank(&amp;g, 0.85, 100, 1e-6, None).unwrap();\n\nfor (node, score) in scores {\n    println!(\"Node {:?} has PageRank {:.4}\", node, score);\n}\n\n// Calculate Degree Centrality for the same graph\nlet degree_scores = degree_centrality(&amp;g, DegreeDirection::Successors);\nprintln!(\"Degree Centrality (Successors): {:?}\", degree_scores);\n</code></pre>"},{"location":"guide/centrality/#betweenness-centrality","title":"Betweenness Centrality","text":"<p>Betweenness centrality quantifies the influence of a node over the flow of information between other nodes. It counts the fraction of shortest paths that pass through a node.</p>"},{"location":"guide/centrality/#use-case","title":"Use Case","text":"<p>Finding bridges or bottlenecks in a network (for example, a critical router in a network topology).</p> <pre><code>use graphina::centrality::betweenness;\n\nlet scores = betweenness(&amp;g);\n</code></pre>"},{"location":"guide/centrality/#degree-centrality","title":"Degree Centrality","text":"<p>The simplest measure: the number of edges connected to a node.</p> <ul> <li>For Directed graphs: often split into In-Degree and Out-Degree.</li> <li>For Undirected graphs: just Degree.</li> </ul> <pre><code>use graphina::centrality::degree;\n\nlet scores = degree(&amp;g);\n</code></pre>"},{"location":"guide/centrality/#eigenvector-centrality","title":"Eigenvector Centrality","text":"<p>Determines importance based on connections to other high-scoring nodes. It is similar to PageRank but without the damping factor/random jump.</p> <pre><code>use graphina::centrality::eigenvector;\n\n// (graph, max_iterations, tolerance)\nlet scores = eigenvector(&amp;g, 100, 1e-6);\n</code></pre>"},{"location":"guide/centrality/#closeness-centrality","title":"Closeness Centrality","text":"<p>node is central if it is close to all other nodes. It is defined as the reciprocal of the sum of shortest path distances.</p> <pre><code>use graphina::centrality::closeness;\n\nlet scores = closeness(&amp;g);\n</code></pre>"},{"location":"guide/community/","title":"Community Detection","text":"<p>Community detection (or clustering) algorithms identify groups of nodes that are more densely connected to each other than to the rest of the network.</p>"},{"location":"guide/community/#label-propagation","title":"Label Propagation","text":"<p>A fast algorithm for finding communities in large networks. Nodes adopt the label that most of their neighbors have.</p>"},{"location":"guide/community/#function-signature","title":"Function Signature","text":"<pre><code>pub fn label_propagation&lt;A, W, Ty&gt;(\n    graph: &amp;BaseGraph&lt;A, W, Ty&gt;,\n    max_iterations: usize\n) -&gt; HashMap&lt;NodeId, usize&gt;\n</code></pre>"},{"location":"guide/community/#example","title":"Example","text":"<pre><code>use graphina::community::label_propagation;\n\nlet communities = label_propagation(&amp;graph, 100);\n\n// Group nodes by community ID\nlet mut groups: HashMap&lt;usize, Vec&lt;NodeId&gt;&gt; = HashMap::new();\nfor (node, comm_id) in communities {\n    groups.entry(comm_id).or_default().push(node);\n}\n</code></pre>"},{"location":"guide/community/#infomap","title":"Infomap","text":"<p>A flow-based method that minimizes the map equation to detect communities. Efficient for understanding flow constraints in networks.</p> <pre><code>use graphina::community::infomap::infomap;\n\n// infomap(graph, max_iterations, optional_seed)\nlet communities_map = infomap(&amp;graph, 100, Some(42)).unwrap();\n</code></pre>"},{"location":"guide/community/#girvan-newman","title":"Girvan-Newman","text":"<p>A hierarchical method that progressively removes edges with high betweenness centrality. Good for small to medium graphs where hierarchy is important.</p> <pre><code>use graphina::community::girvan_newman::girvan_newman;\n\n// girvan_newman(graph, target_communities)\nlet communities = girvan_newman(&amp;graph, 3).unwrap();\n</code></pre>"},{"location":"guide/community/#spectral-clustering","title":"Spectral Clustering","text":"<p>Uses the eigenvectors of the graph Laplacian to partition the graph.</p> <pre><code>use graphina::community::spectral::spectral_clustering;\n\n// spectral_clustering(graph, num_clusters, optional_seed)\nlet communities = spectral_clustering(&amp;graph, 3, Some(42)).unwrap();\n</code></pre>"},{"location":"guide/community/#personalized-pagerank-communities","title":"Personalized PageRank Communities","text":"<p>Detects communities based on random walk proximity to a seed set.</p> <pre><code>use graphina::community::personalized_pagerank::personalized_page_rank;\n\nlet ppr_scores = personalized_page_rank(&amp;graph, None, 0.85, 1e-6, 100).unwrap();\n</code></pre>"},{"location":"guide/community/#louvain-method","title":"Louvain Method","text":"<p>A heuristic method to extract communities by optimizing modularity. It is widely considered one of the best algorithms for community detection due to its speed and quality of results.</p> <pre><code>use graphina::community::louvain;\n\n// Returns mapping from NodeId -&gt; Community ID\nlet communities = louvain(&amp;graph);\n</code></pre>"},{"location":"guide/community/#connected-components","title":"Connected Components","text":"<p>Finds isolated subgraphs where every node is reachable from every other node.</p> <ul> <li>Weakly Connected: Ignoring edge direction.</li> <li>Strongly Connected: Respecting edge direction (every node must reach every other node).</li> </ul> <pre><code>use graphina::community::connected_components;\n\nlet components = connected_components(&amp;graph);\nprintln!(\"Found {} components\", components.len());\n</code></pre>"},{"location":"guide/generators/","title":"Graph Generators","text":"<p>Graphina provides generators for creating synthetic graphs with specific properties, useful for testing and analysis.</p>"},{"location":"guide/generators/#random-graph-models","title":"Random Graph Models","text":""},{"location":"guide/generators/#erdos-renyi-er","title":"Erd\u0151s-R\u00e9nyi (ER)","text":"<p>Generates a random graph where each edge exists with probability <code>p</code>.</p> <pre><code>use graphina::core::generators::erdos_renyi_graph;\nuse graphina::core::types::Undirected;\n\n// 100 nodes, edge probability 0.1, seed 42\nlet g = erdos_renyi_graph::&lt;Undirected&gt;(100, 0.1, 42).unwrap();\n</code></pre>"},{"location":"guide/generators/#barabasi-albert-ba","title":"Barab\u00e1si-Albert (BA)","text":"<p>Generates a scale-free network using preferential attachment. New nodes attach to existing high-degree nodes.</p> <pre><code>use graphina::core::generators::barabasi_albert_graph;\nuse graphina::core::types::Undirected;\n\n// 1000 nodes, each new node adds 3 edges, seed 42\nlet g = barabasi_albert_graph::&lt;Undirected&gt;(1000, 3, 42).unwrap();\n</code></pre>"},{"location":"guide/generators/#watts-strogatz-ws","title":"Watts-Strogatz (WS)","text":"<p>Generates a small-world network by rewiring a regular lattice ring lattice.</p> <pre><code>use graphina::core::generators::watts_strogatz_graph;\nuse graphina::core::types::Undirected;\n\n// 100 nodes, k=4 nearest neighbors, rewiring probability 0.1, seed 42\nlet g = watts_strogatz_graph::&lt;Undirected&gt;(100, 4, 0.1, 42).unwrap();\n</code></pre>"},{"location":"guide/generators/#determinism","title":"Determinism","text":"<p>All generators accept a <code>seed</code> parameter (u64). Using the same seed guarantees the same graph structure, ensuring reproducibility for tests and experiments.</p>"},{"location":"guide/generators/#common-use-cases","title":"Common Use Cases","text":"<ul> <li>Benchmarking: Test algorithm performance on graphs of increasing size.</li> <li>Null Models: Compare properties of real-world networks against random baselines (e.g., \"Is this community structure significant compared to an ER graph?\").</li> <li>Simulations: Model disease spread or information diffusion on realistic topologies (BA or WS).</li> </ul>"},{"location":"guide/graphs/","title":"Graph Types in Graphina","text":"<p>Graphina provides robust, statically typed graph structures. Select the appropriate type for your use case.</p>"},{"location":"guide/graphs/#main-types","title":"Main Types","text":"<p>Graphina focuses on simple graphs to maximize performance. Multiple edges between the same node pair are not supported.</p>"},{"location":"guide/graphs/#grapha-w-undirected","title":"<code>Graph&lt;A, W&gt;</code> (Undirected)","text":"<p><code>Graph</code> represents an undirected graph.</p> <ul> <li>Edges: Bidirectional. Adding an edge from <code>a</code> to <code>b</code> implies a connection <code>b</code> to <code>a</code>.</li> <li>Use Cases: Social networks, road networks, molecular structures.</li> </ul> <pre><code>use graphina::core::types::Graph;\n\n// Nodes store &amp;str, Edges store f64 weights\nlet mut g = Graph::&lt;&amp;str, f64&gt;::new();\n</code></pre>"},{"location":"guide/graphs/#digrapha-w-directed","title":"<code>Digraph&lt;A, W&gt;</code> (Directed)","text":"<p><code>Digraph</code> represents a directed graph.</p> <ul> <li>Edges: Directional. An edge from <code>a</code> to <code>b</code> does not imply <code>b</code> to <code>a</code>.</li> <li>Use Cases: Web pages, citation networks, dependency graphs.</li> </ul> <pre><code>use graphina::core::types::Digraph;\n\nlet mut dg = Digraph::&lt;&amp;str, f64&gt;::new();\n</code></pre>"},{"location":"guide/graphs/#performance-and-memory-layout","title":"Performance and Memory Layout","text":"<p>Graphina uses a <code>StableGraph</code> backend from <code>petgraph</code>.</p> <ol> <li>Vector-backed storage: Nodes and edges use <code>Vec</code> structures.</li> <li>Stable Indices: Removing a node does not shift other indices. Safely retain <code>NodeId</code>s.</li> <li>Cache Locality: Contiguous memory usage improves iteration performance.</li> </ol>"},{"location":"guide/graphs/#nodeid-vs-node-values","title":"NodeId vs Node Values","text":"<p>NetworkX adds nodes by value:</p> <pre><code>G.add_node(\"Alice\")\nG.add_edge(\"Alice\", \"Bob\")\n</code></pre> <p>Graphina separates topology from data. \"Alice\" is an attribute; the node is identified by a lightweight <code>NodeId</code>.</p> <pre><code>let alice_id = graph.add_node(\"Alice\");\nlet bob_id = graph.add_node(\"Bob\");\n\n// Connect using IDs, not strings\ngraph.add_edge(alice_id, bob_id, 1.0);\n</code></pre> <p>This design separates topology from data, enabling optimized integer-based algorithms.</p>"},{"location":"guide/graphs/#density","title":"Density","text":"<p>Check density (ratio of existing to possible edges).</p> <pre><code>let d = graph.density();\nprintln!(\"Graph density: {:.2}\", d);\n</code></pre>"},{"location":"guide/io/","title":"Graph I/O","text":"<p>Graphina supports multiple formats for saving and loading graphs, facilitating data interchange and persistence.</p>"},{"location":"guide/io/#supported-formats","title":"Supported Formats","text":"Format Read Write Best For Edge List Yes Yes Simple, text-based data exchange. Adjacency List Yes Yes Compact text representation. JSON Yes Yes Web applications and detailed attribute storage. Binary Yes Yes High-performance storage for large graphs. GraphML No Yes Interoperability with Gephi, Cytoscape, etc."},{"location":"guide/io/#text-formats","title":"Text Formats","text":""},{"location":"guide/io/#edge-list","title":"Edge List","text":"<p>Reads/Writes a list of edges, one per line.</p> <pre><code>use graphina::core::types::Graph;\nuse graphina::core::io::{read_edge_list, write_edge_list};\n\n// Save\nwrite_edge_list(&amp;graph, \"graph.txt\", \" \").unwrap();\n\n// Load (Node type i32, Edge weight f64)\nlet g: Graph&lt;i32, f64&gt; = read_edge_list(\"graph.txt\", \" \").unwrap();\n</code></pre>"},{"location":"guide/io/#adjacency-list","title":"Adjacency List","text":"<p>Reads/Writes an adjacency list format (Node Neighbor1 Neighbor2 ...).</p> <pre><code>use graphina::core::io::{read_adjacency_list, write_adjacency_list};\n\nwrite_adjacency_list(&amp;graph, \"adj.txt\").unwrap();\nlet g: Graph&lt;i32, f64&gt; = read_adjacency_list(\"adj.txt\").unwrap();\n</code></pre>"},{"location":"guide/io/#serialization","title":"Serialization","text":"<p>Graphina uses <code>serde</code> for serialization.</p>"},{"location":"guide/io/#json","title":"JSON","text":"<pre><code>use graphina::core::serialization::{save_json,load_json};\n\n// Save\nsave_json(&amp;graph, \"graph.json\").unwrap();\n\n// Load\nlet g: Graph&lt;String, f64&gt; = load_json(\"graph.json\").unwrap();\n</code></pre>"},{"location":"guide/io/#binary-bincode","title":"Binary (bincode)","text":"<p>Compact and fast binary format.</p> <pre><code>use graphina::core::serialization::{save_binary, load_binary};\n\nsave_binary(&amp;graph, \"graph.bin\").unwrap();\nlet g: Graph&lt;i32, f64&gt; = load_binary(\"graph.bin\").unwrap();\n</code></pre>"},{"location":"guide/io/#graphml-export-only","title":"GraphML (Export Only)","text":"<p>GraphML is standard for graph visualization tools.</p> <pre><code>use graphina::core::serialization::save_graphml;\n\nsave_graphml(&amp;graph, \"graph.graphml\").unwrap();\n</code></pre>"},{"location":"guide/links/","title":"Link Prediction","text":"<p>Link prediction algorithms estimate the likelihood of a connection between two nodes based on their structural properties.</p>"},{"location":"guide/links/#similarity-measures","title":"Similarity Measures","text":""},{"location":"guide/links/#jaccard-coefficient","title":"Jaccard Coefficient","text":"<p>The Jaccard coefficient measures similarity between two nodes' interaction sets.</p> \\[ J(u, v) = \\frac{|N(u) \\cap N(v)|}{|N(u) \\cup N(v)|} \\] <pre><code>use graphina::links::similarity::jaccard_coefficient;\n\n// Calculate for all pairs\nlet predictions = jaccard_coefficient(&amp;graph, None);\n\n// Or for specific pairs\nlet pairs = vec![(n1, n2)];\nlet predictions = jaccard_coefficient(&amp;graph, Some(&amp;pairs));\n</code></pre>"},{"location":"guide/links/#adamic-adar-index","title":"Adamic-Adar Index","text":"<p>The Adamic-Adar index is a measure that assigns more weight to neighbors with lower degrees.</p> \\[ A(u, v) = \\sum_{w \\in N(u) \\cap N(v)} \\frac{1}{\\log |N(w)|} \\] <pre><code>use graphina::links::similarity::adamic_adar_index;\n\nlet predictions = adamic_adar_index(&amp;graph, None);\n</code></pre>"},{"location":"guide/links/#common-neighbors","title":"Common Neighbors","text":"<p>Simply counts the number of shared neighbors between two nodes.</p> <pre><code>use graphina::links::similarity::common_neighbors;\n\nlet count = common_neighbors(&amp;graph, n1, n2);\n</code></pre>"},{"location":"guide/links/#resource-allocation","title":"Resource Allocation","text":""},{"location":"guide/links/#resource-allocation-index","title":"Resource Allocation Index","text":"<p>Penalizes contributions from high-degree common neighbors.</p> <pre><code>use graphina::links::allocation::resource_allocation_index;\n\nlet predictions = resource_allocation_index(&amp;graph, None);\n</code></pre>"},{"location":"guide/links/#ra-soundarajan-hopcroft","title":"RA Soundarajan-Hopcroft","text":"<p>Resource allocation index incorporating community information.</p> <pre><code>use graphina::links::allocation::ra_index_soundarajan_hopcroft;\n\n// Define a community closure\nlet community_map = |node_id| 0; // dummy\nlet predictions = ra_index_soundarajan_hopcroft(&amp;graph, None, community_map);\n</code></pre>"},{"location":"guide/links/#preferential-attachment","title":"Preferential Attachment","text":"<p>Measures the likelihood of a link based on the product of node degrees (rich get richer).</p> <pre><code>use graphina::links::attachment::preferential_attachment;\n\nlet predictions = preferential_attachment(&amp;graph, None);\n</code></pre>"},{"location":"guide/links/#common-neighbor-centrality","title":"Common Neighbor Centrality","text":"<p>A parameterized centrality measure based on common neighbors.</p> <pre><code>use graphina::links::centrality::common_neighbor_centrality;\n\nlet alpha = 0.8;\nlet predictions = common_neighbor_centrality(&amp;graph, None, alpha);\n</code></pre>"},{"location":"guide/links/#community-based","title":"Community-Based","text":""},{"location":"guide/links/#cn-soundarajan-hopcroft","title":"CN Soundarajan-Hopcroft","text":"<p>Counts common neighbors that belong to the same community as the source and target.</p> <pre><code>use graphina::links::soundarajan_hopcroft::cn_soundarajan_hopcroft;\n\nlet predictions = cn_soundarajan_hopcroft(&amp;graph, None, |n| /* return community ID */ 0);\n</code></pre>"},{"location":"guide/links/#within-inter-cluster","title":"Within-Inter Cluster","text":"<p>Ratio of within-cluster common neighbors to inter-cluster common neighbors.</p> <pre><code>use graphina::links::cluster::within_inter_cluster;\n\nlet predictions = within_inter_cluster(&amp;graph, None, |n| 0, 0.01);\n</code></pre>"},{"location":"guide/metrics/","title":"Graph Metrics","text":"<p>Graph metrics provide quantitative measures of the structure and properties of a graph.</p>"},{"location":"guide/metrics/#distance-metrics","title":"Distance Metrics","text":""},{"location":"guide/metrics/#diameter","title":"Diameter","text":"<p>The diameter is the longest shortest path between any two nodes in the graph.</p> <pre><code>use graphina::metrics::diameter;\n\nlet d = diameter(&amp;graph);\n// Returns Option&lt;usize&gt;, None if disconnected\n</code></pre>"},{"location":"guide/metrics/#radius","title":"Radius","text":"<p>The radius is the minimum eccentricity of any node in the graph. The eccentricity of a node is the maximum distance from it to any other node.</p> <pre><code>use graphina::metrics::radius;\n\nlet r = radius(&amp;graph);\n</code></pre>"},{"location":"guide/metrics/#average-path-length","title":"Average Path Length","text":"<p>The average length of shortest paths between all pairs of nodes.</p> <pre><code>use graphina::metrics::average_path_length;\n\nlet avg_len = average_path_length(&amp;graph);\n</code></pre>"},{"location":"guide/metrics/#clustering-and-mixing","title":"Clustering and Mixing","text":""},{"location":"guide/metrics/#clustering-coefficient","title":"Clustering Coefficient","text":"<p>Measures the degree to which nodes tend to cluster together.</p> <pre><code>use graphina::metrics::average_clustering_coefficient;\n\nlet clustering = average_clustering_coefficient(&amp;graph);\n</code></pre>"},{"location":"guide/metrics/#transitivity","title":"Transitivity","text":"<p>Also known as the global clustering coefficient, it measures the ratio of triangles to connected triples.</p> <pre><code>use graphina::metrics::transitivity;\n\nlet t = transitivity(&amp;graph);\n</code></pre>"},{"location":"guide/metrics/#assortativity","title":"Assortativity","text":"<p>Measures the tendency of nodes to connect to others with similar degrees. Returns a value between -1 (disassortative) and 1 (assortative).</p> <pre><code>use graphina::metrics::assortativity;\n\nlet r = assortativity(&amp;graph);\n</code></pre>"},{"location":"guide/mst/","title":"Minimum Spanning Tree (MST)","text":"<p>Minimum Spanning Tree algorithms find a subset of edges that connect all nodes in a graph with the minimum possible total edge weight, without forming any cycles.</p>"},{"location":"guide/mst/#algorithms","title":"Algorithms","text":""},{"location":"guide/mst/#prims-algorithm","title":"Prim's Algorithm","text":"<p>Prim's algorithm grows the MST from a starting node, adding the cheapest edge at each step.</p> <pre><code>use graphina::core::types::{Graph, NodeId};\nuse graphina::mst::prim_mst;\nuse ordered_float::OrderedFloat;\n\nfn main() {\n    let mut g = Graph::&lt;i32, OrderedFloat&lt;f64&gt;&gt;::new();\n    let n1 = g.add_node(1);\n    let n2 = g.add_node(2);\n    let n3 = g.add_node(3);\n\n    g.add_edge(n1, n2, OrderedFloat(1.0));\n    g.add_edge(n1, n3, OrderedFloat(3.0));\n    g.add_edge(n2, n3, OrderedFloat(2.0)); // Cheaper path via n2\n\n    let (mst_edges, total_weight) = prim_mst(&amp;g).unwrap();\n    println!(\"Total Weight: {}\", total_weight);\n}\n</code></pre>"},{"location":"guide/mst/#kruskals-algorithm","title":"Kruskal's Algorithm","text":"<p>Kruskal's algorithm sorts all edges by weight and adds them if they don't form a cycle.</p> <pre><code>use graphina::mst::kruskal_mst;\n\nlet (mst_edges, total_weight) = kruskal_mst(&amp;g).unwrap();\n</code></pre>"},{"location":"guide/mst/#boruvkas-algorithm-parallel","title":"Bor\u016fvka's Algorithm (Parallel)","text":"<p>A parallel implementation that is efficient for large graphs. It works by finding the minimum weight edge incident to each component in parallel.</p> <p>Parallel Feature</p> <p>Requires the <code>parallel</code> feature and <code>W</code> must implement <code>Send + Sync</code>.</p> <pre><code>use graphina::mst::boruvka_mst;\n\nlet (mst_edges, total_weight) = boruvka_mst(&amp;g).unwrap();\n</code></pre>"},{"location":"guide/mst/#weight-type-requirements","title":"Weight Type Requirements","text":"<p>Edge weights <code>W</code> must implement <code>Ord</code>. For floating point numbers, use <code>OrderedFloat</code> or a similar wrapper to provide total ordering.</p> <pre><code>use ordered_float::OrderedFloat;\nlet mut g = Graph::&lt;i32, OrderedFloat&lt;f64&gt;&gt;::new();\nlet u = g.add_node(1);\nlet v = g.add_node(2);\ng.add_edge(u, v, OrderedFloat(1.5));\n</code></pre>"},{"location":"guide/parallel/","title":"Parallel Processing","text":"<p>Graphina provides multi-threaded implementations of common algorithms using Rayon. This allows you to leverage all CPU cores for processing large graphs.</p>"},{"location":"guide/parallel/#enabling-parallel-support","title":"Enabling Parallel Support","text":"<p>The parallel feature must be enabled in your <code>Cargo.toml</code> (if not enabled by default):</p> <pre><code>[dependencies]\ngraphina = { version = \"0.3.0-alpha.4\", features = [\"parallel\"] }\n</code></pre>"},{"location":"guide/parallel/#available-algorithms","title":"Available Algorithms","text":"<p>The <code>graphina::parallel</code> module mirrors many algorithms from the main modules but executes them in parallel.</p>"},{"location":"guide/parallel/#parallel-pagerank","title":"Parallel PageRank","text":"<p>Significantly faster than the single-threaded version for graphs with millions of nodes.</p> <pre><code>use graphina::core::types::Digraph;\nuse graphina::parallel::pagerank_parallel;\n\nlet mut g = Digraph::&lt;&amp;str, f64&gt;::new();\n// Add some nodes\nlet n1 = g.add_node(\"A\");\nlet n2 = g.add_node(\"B\");\ng.add_edge(n1, n2, 1.0);\n\nlet ranks = pagerank_parallel(&amp;g, 0.85, 100, 1e-6, None);\n</code></pre>"},{"location":"guide/parallel/#parallel-connected-components","title":"Parallel Connected Components","text":"<p>Finds connected components in parallel.</p> <pre><code>use graphina::parallel::components::connected_components_parallel;\n\nlet components = connected_components_parallel(&amp;graph);\n</code></pre>"},{"location":"guide/parallel/#parallel-breadth-first-search-bfs","title":"Parallel Breadth-First Search (BFS)","text":"<p>Performing BFS from multiple sources concurrently.</p> <pre><code>use graphina::parallel::bfs::bfs_parallel;\n\nlet mut g = Digraph::&lt;i32, f64&gt;::new();\nlet start_nodes = vec![n1, n2];\nlet visited = bfs_parallel(&amp;g, start_nodes);\n</code></pre>"},{"location":"guide/parallel/#when-to-use-parallelism","title":"When to use Parallelism?","text":"<p>Parallelism implies overhead. Use it when:</p> <ul> <li>The graph has &gt; 100,000 nodes.</li> <li>The algorithm is computationally intensive (e.g., Betweenness Centrality).</li> </ul>"},{"location":"guide/parallel/#thread-safety","title":"Thread Safety","text":"<p>Graphina's <code>Graph</code> and <code>Digraph</code> types are thread-safe (implement <code>Sync</code>) as long as the node attributes (<code>A</code>) and edge weights (<code>W</code>) are also <code>Sync</code>. This allows them to be shared across threads efficiently.</p>"},{"location":"guide/paths/","title":"Path Finding","text":"<p>Graphina provides a suite of algorithms for finding shortest paths and traversing graphs.</p>"},{"location":"guide/paths/#dijkstras-algorithm","title":"Dijkstra's Algorithm","text":"<p>Finds the shortest paths from a source node to all other nodes (or a target node) in a graph with non-negative edge weights.</p>"},{"location":"guide/paths/#function-signature","title":"Function Signature","text":"<pre><code>pub fn dijkstra&lt;A, W, Ty&gt;(\n    graph: &amp;BaseGraph&lt;A, W, Ty&gt;,\n    source: NodeId\n) -&gt; Result&lt;NodeMap&lt;Option&lt;W&gt;&gt;&gt;\n</code></pre>"},{"location":"guide/paths/#example","title":"Example","text":"<pre><code>use graphina::core::paths::dijkstra;\n\nlet result = dijkstra(&amp;graph, start_node)?;\n\nif let Some(cost) = result.get(&amp;end_node).unwrap() {\n    println!(\"Shortest distance: {}\", cost);\n} else {\n    println!(\"Node is unreachable\");\n}\n</code></pre>"},{"location":"guide/paths/#a-a-star-search","title":"A* (A-Star) Search","text":"<p>Finds the shortest path to a specific target using a heuristic function to guide the search. Faster than Dijkstra if you have a good heuristic (like Euclidean distance for maps).</p> <pre><code>use graphina::core::paths::a_star;\n\n// Heuristic function: estimate distance from u to target\nlet heuristic = |u: NodeId| -&gt; f64 {\n    // Calculate distance\n    let dist = (x1 - x2).hypot(y1 - y2);\n    0.0\n};\n\nlet path = a_star(&amp;graph, start, end, heuristic)?;\n</code></pre>"},{"location":"guide/paths/#bellman-ford","title":"Bellman-Ford","text":"<p>Computes shortest paths from a single source in graphs that may contain negative edge weights. It can also detect negative cycles.</p> <pre><code>use graphina::core::paths::bellman_ford;\n\nmatch bellman_ford(&amp;graph, start_node) {\n    Some(distances) =&gt; println!(\"Calculated distances\"),\n    None =&gt; println!(\"Negative cycle detected!\"),\n}\n</code></pre>"},{"location":"guide/paths/#floyd-warshall","title":"Floyd-Warshall","text":"<p>Computes all-pairs shortest paths. Returns a matrix (map of maps) of distances. Note: This is \\(O(V^3)\\), so use only on small graphs (&lt; 500-1000 nodes).</p> <pre><code>use graphina::core::paths::floyd_warshall;\n\nlet all_paths = floyd_warshall(&amp;graph);\n</code></pre>"},{"location":"guide/subgraphs/","title":"Subgraphs and Views","text":"<p>Graphina provides a rich set of operations to extract subgraphs, filter views, and analyze local neighborhoods. These methods are available via the <code>SubgraphOps</code> trait.</p>"},{"location":"guide/subgraphs/#extracting-subgraphs","title":"Extracting Subgraphs","text":""},{"location":"guide/subgraphs/#induced-subgraph","title":"Induced Subgraph","text":"<p>Create a new graph containing only the specified nodes and all edges between them.</p> <pre><code>use graphina::subgraphs::SubgraphOps;\n\nlet nodes = vec![n1, n2];\nlet subgraph = graph.subgraph(&amp;nodes).unwrap();\n</code></pre>"},{"location":"guide/subgraphs/#ego-graph","title":"Ego Graph","text":"<p>Extract the neighborhood around a center node up to a certain radius.</p> <pre><code>// 1-hop neighborhood around n1\nlet ego = graph.ego_graph(n1, 1).unwrap();\n</code></pre>"},{"location":"guide/subgraphs/#connected-components","title":"Connected Components","text":"<p>Extract the connected component containing a specific node.</p> <pre><code>let component = graph.component_subgraph(n1).unwrap();\n</code></pre>"},{"location":"guide/subgraphs/#filtering","title":"Filtering","text":"<p>Create subgraphs by filtering nodes or edges based on custom predicates.</p> <pre><code>// Keep only nodes with even IDs\nlet even_nodes = graph.filter_nodes(|id, _attr| id.index() % 2 == 0);\n\n// Keep only edges with weight &gt; 10.0\nlet heavy_edges = graph.filter_edges(|_u, _v, w| *w &gt; 10.0);\n</code></pre>"},{"location":"guide/subgraphs/#neighborhoods","title":"Neighborhoods","text":"<p>Query local structure without creating a new graph object.</p> <pre><code>// Get all nodes within 2 hops\nlet neighbors = graph.k_hop_neighbors(n1, 2);\n</code></pre>"},{"location":"guide/traversal/","title":"Graph Traversal","text":"<p>Traverse graphs using standard algorithms like Breadth-First Search (BFS) and Depth-First Search (DFS).</p>"},{"location":"guide/traversal/#basic-traversals","title":"Basic Traversals","text":""},{"location":"guide/traversal/#breadth-first-search-bfs","title":"Breadth-First Search (BFS)","text":"<p>Explores the graph layer by layer, visiting all neighbors of a node before moving to the next level.</p> <pre><code>use graphina::traversal::bfs;\n\nlet order = bfs(&amp;graph, start_node);\nprintln!(\"Visist order: {:?}\", order);\n</code></pre>"},{"location":"guide/traversal/#depth-first-search-dfs","title":"Depth-First Search (DFS)","text":"<p>Explores as far as possible along each branch before backtracking.</p> <pre><code>use graphina::traversal::dfs;\n\nlet order = dfs(&amp;graph, start_node);\n</code></pre>"},{"location":"guide/traversal/#advanced-algorithms","title":"Advanced Algorithms","text":""},{"location":"guide/traversal/#iterative-deepening-dfs-iddfs","title":"Iterative Deepening DFS (IDDFS)","text":"<p>Combines the space efficiency of DFS with the optimality of BFS. It repeatedly runs DFS with increasing depth limits.</p> <pre><code>use graphina::traversal::iddfs;\n\n// Find path with max depth limit of 10\nif let Some(path) = iddfs(&amp;graph, start, target, 10) {\n    println!(\"Found path: {:?}\", path);\n}\n</code></pre>"},{"location":"guide/traversal/#bidirectional-search","title":"Bidirectional Search","text":"<p>Runs two simultaneous searches: one forward from the source and one backward from the target, stopping when they meet. This can be significantly faster than standard BFS for finding shortest paths.</p> <pre><code>use graphina::traversal::bidis;\n\nif let Some(path) = bidis(&amp;graph, start, target) {\n    println!(\"Shortest path: {:?}\", path);\n}\n</code></pre>"},{"location":"guide/visualization/","title":"Visualization","text":"<p>Graphina provides tools to visualize graphs directly or export them to other formats.</p>"},{"location":"guide/visualization/#ascii-visualization","title":"ASCII Visualization","text":"<p>For small graphs or quick debugging, you can print the graph structure to the console.</p> <pre><code>use graphina::core::types::Graph;\nuse graphina::visualization::d3::D3Graph;\n\n// Setup\nlet mut graph = Graph::&lt;&amp;str, f64&gt;::new();\ngraph.add_node(\"A\");\nprintln!(\"{}\", graph.to_ascii_art());\n</code></pre>"},{"location":"guide/visualization/#interactive-html-d3js","title":"Interactive HTML / D3.js","text":"<p>Graphina can generate a standalone HTML file containing an interactive force-directed graph visualization powered by D3.js.</p> <pre><code>use graphina::visualization::config::VisualizationConfig;\nuse graphina::visualization::d3::BaseGraphExt; // Import trait extensions if needed\n\nlet config = VisualizationConfig::default();\n\n// Save to file\ng.save_as_html(\"graph_view.html\", &amp;config).expect(\"Failed to save HTML\");\n</code></pre> <p>Open <code>graph_view.html</code> in your browser to explore the graph (zoom, pan, drag nodes).</p>"},{"location":"guide/visualization/#exporting-for-other-tools","title":"Exporting for Other Tools","text":""},{"location":"guide/visualization/#dot-graphviz","title":"DOT / Graphviz","text":"<p>Graphina plans to support DOT export. Check the roadmap for updates.</p>"},{"location":"guide/visualization/#d3-json","title":"D3 JSON","text":"<p>You can export the raw JSON structure required for D3.js if you want to build a custom frontend.</p> <pre><code>let json_string = g.to_d3_json().unwrap();\n</code></pre>"}]}